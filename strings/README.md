In console.log(``) to use `` because variables could be passed easily

string supports multiple methods, below link could be referred for same

link : https://www.w3schools.com/js/js_string_methods.asp